# ArgoCD Sync Workflow
# Triggers ArgoCD sync when infrastructure changes are pushed
name: Sync ArgoCD

on:
  push:
    branches: [main]
    paths:
      - 'base/**'
      - 'overlays/**'
      - 'services/**'
      - 'argocd/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to sync'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
          - all

jobs:
  sync-staging:
    name: Sync Staging
    runs-on: [self-hosted, staging, goapps-runner]
    if: github.event_name == 'push' || github.event.inputs.environment == 'staging' || github.event.inputs.environment == 'all'
    environment: staging
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ArgoCD CLI
        uses: ./.github/actions/install-argocd-cli

      - name: Login to ArgoCD
        run: |
          argocd login ${{ secrets.ARGOCD_SERVER_STAGING }} \
            --username admin \
            --password ${{ secrets.ARGOCD_PASSWORD_STAGING }} \
            --grpc-web \
            --insecure

      - name: Sync Applications
        run: |
          echo "🔄 Syncing staging applications..."
          argocd app sync infra-apps --prune || true
          argocd app sync finance-service-staging --prune || true
          echo "✅ Staging sync triggered"

      - name: Wait for Healthy
        run: |
          echo "⏳ Waiting for applications to be healthy..."
          argocd app wait infra-apps --timeout 300 || true
          argocd app wait finance-service-staging --timeout 300 || true
          echo "✅ Applications healthy"

  sync-production:
    name: Sync Production
    runs-on: [self-hosted, production, goapps-runner]
    # Only run for 'all' environment selection
    if: github.event.inputs.environment == 'all'
    environment: production
    needs: [sync-staging]
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ArgoCD CLI
        uses: ./.github/actions/install-argocd-cli

      - name: Start Port Forward
        run: |
          echo "🔌 Starting port-forward to ArgoCD..."
          kubectl port-forward svc/argocd-server -n argocd 8443:443 &
          sleep 3
          echo "✅ Port-forward ready on localhost:8443"

      - name: Login to ArgoCD
        run: |
          argocd login localhost:8443 \
            --username admin \
            --password ${{ secrets.ARGOCD_PASSWORD_PRODUCTION }} \
            --insecure

      - name: Sync Applications
        run: |
          echo "🔄 Syncing production applications..."
          argocd app sync infra-apps --prune
          argocd app sync finance-service-production --prune
          echo "✅ Production sync triggered"

      - name: Wait for Healthy
        run: |
          echo "⏳ Waiting for applications to be healthy..."
          argocd app wait infra-apps --timeout 300
          argocd app wait finance-service-production --timeout 300
          echo "✅ Applications healthy"

  sync-production-only:
    name: Sync Production (Direct)
    runs-on: [self-hosted, production, goapps-runner]
    # Simplified condition: github.event.inputs.environment only exists for workflow_dispatch
    # so checking workflow_dispatch explicitly is redundant
    if: github.event.inputs.environment == 'production'
    environment: production
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ArgoCD CLI
        uses: ./.github/actions/install-argocd-cli

      - name: Start Port Forward
        run: |
          echo "🔌 Starting port-forward to ArgoCD..."
          kubectl port-forward svc/argocd-server -n argocd 8443:443 &
          sleep 3
          echo "✅ Port-forward ready on localhost:8443"

      - name: Login to ArgoCD
        run: |
          argocd login localhost:8443 \
            --username admin \
            --password ${{ secrets.ARGOCD_PASSWORD_PRODUCTION }} \
            --insecure

      - name: Sync Applications
        run: |
          echo "🔄 Syncing production applications (direct)..."
          argocd app sync infra-apps --prune
          argocd app sync finance-service-production --prune
          echo "✅ Production sync triggered"

      - name: Wait for Healthy
        run: |
          echo "⏳ Waiting for applications to be healthy..."
          argocd app wait infra-apps --timeout 300
          argocd app wait finance-service-production --timeout 300
          echo "✅ Applications healthy"
