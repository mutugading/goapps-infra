# ArgoCD Application - Infrastructure Backup (Production)
# Uses production overlay with /goapps-backup paths
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infra-backup
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: goapps
  source:
    repoURL: https://github.com/mutugading/goapps-infra.git
    targetRevision: main
    path: overlays/production/backup
  destination:
    server: https://kubernetes.default.svc
    namespace: database
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
